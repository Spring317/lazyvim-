return {
  "folke/snacks.nvim",
  opts = {
    animate = { enabled = false }, -- Disables window/split animations
    scroll = { enabled = false }, -- Disables smooth scrolling (makes jumps instant)
    indent = {
      animate = { enabled = false }, -- Disables scope line animations
    },
    notifier = { enabled = false }, -- Disables floating notifications
  },
}
